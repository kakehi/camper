<div class="post-list">
  <a class="post" href="<%= edit_organization_path(@organization) %>">
    <div class="header">
      <h2 class="title"><%= @organization.name %></h2>
    </div>
  </a>
</div>
<div class="post-list">
  <a class="post" href="<%= edit_camp_path(@camp) %>">
    <div class="header">
      <h2 class="title"><%= @camp.name %></h2>
    </div>
  </a>
</div>

<div class="view-outer-container single-column">
  <div class="row">
    <div class="col-md-8 mx-auto form-container">
      <h2 class="h2">New activity</h2>
      <%= form_with(url: create_activity_path, model: @activity, local: true, method: :post, html: { class: 'activity-form' } ) do |f| %>

        <div class="form-group">
          <%= f.label "Name", class: 'form-label' %>
          <%= f.text_field :name, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label "Description", class: 'form-label' %>
          <%= f.text_area :description, class: 'form-control' %>
        </div>

        <div class="form-group image-upload">
          <%= f.label "Hero image", class: 'form-label' %>
          <div class="image-preview" id="imagePreview">
            <img src="" alt="Image Preview" class="image-preview__image">
            <span class="image-preview__default-text">Image will be displayed here</span>
          </div>
          <%= f.file_field :hero_image, class: 'form-control-file', id: 'imageUpload' %>
        </div>

        <div class="form-group">
            <h4>Dates</h4>
            <%= f.label "Start year", class: 'form-label' %>
            <%= f.text_field :start_year, class: 'form-control' %>
            <%= f.label "Start month", class: 'form-label' %>
            <%= f.text_field :start_month, class: 'form-control' %>
            <%= f.label "Start date", class: 'form-label' %>
            <%= f.text_field :start_date, class: 'form-control' %>
            <%= f.label "End year", class: 'form-label' %>
            <%= f.text_field :end_year, class: 'form-control' %>
            <%= f.label "End month", class: 'form-label' %>
            <%= f.text_field :end_month, class: 'form-control' %>
            <%= f.label "End date", class: 'form-label' %>
            <%= f.text_field :end_date, class: 'form-control' %>
        </div>

        <div class="button-group">
          <%= f.submit 'Create', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
