<div class="container">
  <div class="row">
    <div class="post-list">
        <a class="post" href="<%= edit_organization_path(@organization) %>">
            <div class="header">
            <h2 class="title"><%= @organization.name %></h2>
            <p class="timestamp"><%= @organization.created_at.strftime("%B %d, %Y") %></p>
            </div>
            <div class="body">
            <p class="content"><%= @organization.description %></p>
            </div>
            <div class="image">
            <%= image_tag(@organization.hero_image) if @organization.hero_image.attached? %>
            </div>
        </a>
    </div>
    <div class="col-md-8 mx-auto form-container">
      <h2 class="h2">Edit camp</h2>
      <%= form_with(url: update_camp_path, model: @camp, local: true, method: :post, html: { class: 'post-form' }) do |f| %>

        <% if @camp.errors.any? %>
          <ul>
            <% @camp.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %> </ul>
        <% end %>

        <div class="form-group">
          <%= f.label "Name", class: 'form-label' %>
          <%= f.text_field :name, value: @camp.name, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label "Description", class: 'form-label' %>
          <%= f.text_area :description, value: @camp.description, class: 'form-control' %>
        </div>

        <div class="form-group image-upload">
          <%= f.label "画像", class: 'form-label' %>
          <div class="image-preview" id="imagePreview">
            <img src="<%= @camp.hero_image.attached? ? url_for(@camp.hero_image) : "" %>" alt="Image Preview" class="image-preview__image <%= @camp.hero_image.attached? ? "-block" : "" %>">
            <span class="image-preview__default-text <%= @camp.hero_image.attached? ? "-none" : "" %>">No image uploaded</span>
          </div>
          <%= f.file_field :hero_image, class: 'form-control-file', id: 'imageUpload' %>
        </div>

        <div class="button-group">
          <%= link_to 'Delete', destroy_camp_path(@camp), method: :delete, data: { confirm: 'Are you sure you want to delete?' }, class: "btn btn-danger" %>
          <%= f.submit 'Edit', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>