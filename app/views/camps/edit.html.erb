<div class="view-outer-container single-column">
  
  <%# 
    PROFILE TOP
  %>
  <%= render "components/cards/organization-thumb-card",
    organization: @organization
  %>
  
  <div class="content">
    <%= render "components/titles/page-title",
      title: "Edit camp" %>
    <%= form_with(url: update_camp_path, model: @camp, local: true, method: :post, html: { class: 'form-container' }) do |f| %>

      <% if @camp.errors.any? %>
        <ul>
          <% @camp.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %> </ul>
      <% end %>

      <div class="form-group" id="overview">
        <div class="form-field">
          <%= f.label "Name", class: 'form-label' %>
          <%= f.text_field :name, value: @camp.name, class: 'form-control' %>
        </div>

        <div class="form-field">
          <%= f.label "Description", class: 'form-label' %>
          <%= f.text_area :description, value: @camp.description, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group image-upload">
        <%= f.label "Hero image", class: 'form-label' %>
        <div class="image-preview" id="imagePreview">
          <img src="<%= @camp.hero_image.attached? ? url_for(@camp.hero_image) : "" %>" alt="Image Preview" class="image-preview__image <%= @camp.hero_image.attached? ? "-block" : "" %>">
          <span class="image-preview__default-text <%= @camp.hero_image.attached? ? "-none" : "" %>">No image uploaded</span>
        </div>
        <%= f.file_field :hero_image, class: 'form-control-file', id: 'imageUpload' %>
      </div>

      <div class="form-group">
          <h4>Age groups (Default)</h4>
          <div class="form-field">
            <%= f.label "Minimum age group", class: 'form-label' %>
            <%= f.text_field :age_group_min, value: defined?(@organization.age_group_min) ? @organization.age_group_min : "", class: 'form-control' %>
          </div>
          <div class="form-field">
            <%= f.label "Max age group (optional)", class: 'form-label' %>
            <%= f.text_field :age_group_max, value: defined?(@organization.age_group_max) ? @organization.age_group_max : "", class: 'form-control' %>
            <div class="caption">Please fill this only if your target age group is a range</div>
          </div>
      </div>

      <div class="form-group">
        <h4>Weekly cost (Default)</h4>
        <div class="form-field">
          <%= f.label "Base cost", class: 'form-label' %>
          <%= f.text_field :base_price, class: 'form-control' %>
        </div>
        <div class="form-field">
          <%= f.label "Discounted cost (optional)", class: 'form-label discounted-price-field' %>
          <div class="form-discounted-price">
            <%= f.text_field :discount_price, class: 'form-control' %>
            <div class="description">
              by signing up <%= f.text_field :minimum_discount_week, class: 'form-control line-field' %> weeks or more.
            </div>
          </div>
        </div>
      </div>
      
      <div class="button-group">
        <%= link_to 'Delete', destroy_camp_path(@camp), method: :delete, data: { confirm: 'Are you sure you want to delete?' }, class: "btn btn-danger" %>
        <%= f.submit 'Update', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>